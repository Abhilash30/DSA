1. .upper() capitalizes only the first letter
2. .isupper() boolean to tell if uppercase or not
3. To control how you go through an array eg. sequencially or skipping 2 or 1 use 2 while loops
4. median is the middle term of a sorted array
5. math.ceil() 1.2 ->2